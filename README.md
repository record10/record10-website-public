# Record10 Website Public
This repository hosts the built static files on the `gh-pages` branch. The `main` branch contains the GitHub action workflow to pull from the private repo and build the site.
